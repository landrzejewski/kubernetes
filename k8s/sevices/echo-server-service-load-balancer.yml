apiVersion: v1
kind: Service
metadata:
  name: echo-server-svc
  namespace: training
spec:
  selector:
    app-name: "echo-server"
  type: LoadBalancer
  # clusterIP: 10.111.201.106
  # internalTrafficPolicy: Cluster
  # externalTrafficPolicy: Cluster
  ports:
    - port: 80
      targetPort: http #8080
      # nodePort: 30000